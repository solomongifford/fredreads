name = "fredreads"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".vercel/output/static"
pages_build_command = "npx @cloudflare/next-on-pages@1"

# Note: D1 database bindings are configured in the backend Workers, not Pages
