import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static frontend - all logic in Workers backend
  // Files in public/ are automatically copied to the build output
};

export default nextConfig;
